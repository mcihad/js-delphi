"""JS-Delphi backend package (FastAPI + SQLite + Jinja2).

The backend never *writes* program code: it validates the component model that the
IDE produced, flattens it and lets fixed Jinja2 templates assemble the output files.
"""
